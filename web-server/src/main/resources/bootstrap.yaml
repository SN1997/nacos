server:
    port: 10000

spring:
    application:
        name: web-server
    cloud:
        nacos:
            discovery:
                server-addr: @nacos.config.host@
                namespace: @nacos.config.namespace@
            config:
                prefix: web
                file-extension: yaml
                server-addr: @nacos.config.host@
                namespace: @nacos.config.namespace@
                group: DEFAULT_GROUP
